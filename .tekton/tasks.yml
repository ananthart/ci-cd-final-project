apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: ci-task
spec:
  steps:
    - name: cleanup
      image: alpine
      script: |
        echo "Cleaning workspace..."
        rm -rf *

    - name: test
      image: python:3.9-slim
      script: |
        pip install nose
        nosetests
